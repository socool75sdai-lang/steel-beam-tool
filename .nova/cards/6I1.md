# 6I1 — Integration QA — all four items — read-only

## STATUS
- Column: DONE
- Agent: Validator (inline)
- Critic verdict: n/a (read-only integration card)
- Validator result: PASS — all four items confirmed in browser; tsc 0 errors; build clean
- Evidence: .nova/evidence/rev6/ (6I1-item1-beam-top-effect.png, 6I1-item1-ff-bottom-governs.png, 6I1-item3-column-calc.png, 6I1-item4-mcveigh-theme.png, 6I1-item4-light-theme.png; brace UI 6R4-brace-results.png; PDF 6R5-brace-export.pdf)

## Results
- **Gates:** `npx tsc --noEmit` 0 errors; `npm run build` clean.
- **Item 1 (beam):** PP beam — bottom-flange restraints (2) left Le = 6.00 m unchanged and emitted
  the "no hogging — bottom restraints carry no compression" note; top-flange restraints (2) dropped
  Le 6.00 -> 2.00 m, governing segment 2.00-4.00 m (middle bay, lowest phiMbx), phiMbx rose
  (alpha_s 0.22 -> 0.58). FF beam — bottom-flange restraints govern at the hogging end: governing
  segment 0.00-2.00 m (bottom flange in compression). Governing = lowest-phiMbx, not longest.
- **Item 2 (brace):** Live engine matched the hand-calc EXACTLY (phiNs=1026.0, phiNc=679.8,
  phiMsx=51.03 kN.m, sw delta=0.39 mm). Auto-lightest returned the global lightest across all three
  types and grades (CHS48.3x3.2, CHS, C250, 3.6 kg/m), setting type+grade+size. Three graphs render.
  3-page ASCII PDF downloads (valid %PDF, 3 pages).
- **Item 3:** Column "Show calculations" substituted values match the table line-for-line
  (N*=600.0, M*x=72.00, phiNs=510.3, Cl 8.3.3=4.799, 8.4.2=9.051, 8.4.4=11.651). Brace collapsible
  matches its table. Both toggle correctly.
- **Item 4:** McVeigh theme — summary lines render cream/legible on green; calc-box, table, and
  Recharts text stay dark/legible on white. Light theme unchanged.

**Read-only — no code changes.** Final end-to-end confirmation of every HANDOVER verification gate.

**Gates:** `npx tsc --noEmit` 0 errors; `npm run build` clean; then browser-MCP confirmation:
- **Item 1:** PP beam — bottom-flange restraints no effect on Le; top-flange reduce Le + raise φMbx.
  FF beam — bottom-flange restraints govern at hogging ends. Governing = lowest-φMbx not longest.
- **Item 2:** hand-calc CHS spot-check reproducing N*, governing M*, φNc x/y, Cl. 8.3/8.4 ratios,
  L/360 deflection; auto-lightest global lightest across 3 types × 3 grades; three graphs; 3-page PDF.
- **Item 3:** Column + Brace collapsibles expand/collapse; substituted values match table + PDF.
- **Item 4:** McVeigh theme — panel summary lines cream/legible; calc-box/table/chart text dark;
  Light theme unchanged.

Save screenshots to `.nova/evidence/rev6/`; record paths in STATUS Evidence.

## History
