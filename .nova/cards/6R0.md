# 6R0 — Grey→cream helper text (McVeigh, all tabs) — Item 4

## STATUS
- Column: DONE
- Agent: Implementer (inline)
- Critic verdict: APPROVED — .mc-subtle scoped correctly; only the two grey-on-green summary lines reclassed; calc/table/Recharts untouched; Light theme unchanged
- Validator result: deferred to 6I1 consolidated browser pass
- Evidence: 6I1 McVeigh/Light theme screenshots
- Commit: a6f4d3d

**Files owned:** `src/index.css`; the panel components carrying the flagged grey text
(`ResultsPanel.tsx`, `ColumnResultsPanel.tsx`; Brace panel inherits the same class once it exists).

**Implementation steps:**
1. Add theme-aware utility class (class-based so it cannot bleed onto white backgrounds):
   ```css
   .mc-subtle { color: #4b5563; }                 /* == Tailwind gray-600, Light theme unchanged */
   [data-theme="mcveigh"] .mc-subtle { color: var(--mc-cream); }
   ```
2. On the flagged grey-on-green elements only, replace `text-gray-500`/`text-gray-600` with
   `mc-subtle`: the "Section class: … · fy = … · Le = …" summary line, the deflection-limit
   labels, and panel-level captions (beam `ResultsPanel.tsx`; column `ColumnResultsPanel.tsx`).
3. Do NOT touch: the "Show calculations" box body/italic notes, results-table cells, or Recharts
   axis/legend text — these sit on white and must stay dark.

**Critic checklist:** `.mc-subtle` defined with Light default `#4b5563` + McVeigh `var(--mc-cream)`;
only grey-on-green elements reclassed; no white-bg text reclassed; rule scoped to
`[data-theme="mcveigh"]`; no other CSS rules modified.

**Visual confirmation:** McVeigh theme — beam + column results panels: summary line + deflection
labels cream/legible on green; calc-box body, table cells, Recharts text stay dark on white. Light
theme unchanged.

## History
