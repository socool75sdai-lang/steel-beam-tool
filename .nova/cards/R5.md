# R5 — Rename: index.html title + PDF report header

## STATUS
- Column: DONE
- Agent: single-agent (direct on main)
- Critic verdict: APPROVED (1 cycle)
- TV result: PASS (title browser-confirmed; PDF header verified by code + clean build)

## Items implemented
- HANDOVER #3 — `index.html` `<title>` → "Steel Designer"; PDF report header → "McVeigh Steel Designer".

## Files owned
- `index.html`
- `src/utils/pdfExport.ts`

## What was done
- `index.html`: `<title>steel-beam-tool</title>` → `<title>Steel Designer</title>`.
- `pdfExport.ts`: report header subtitle line `doc.text('Steel Beam Design Report', 10, 18)` →
  `doc.text('McVeigh Steel Designer', 10, 18)`. No diagram/draw logic touched.

## Acceptance checklist
- [x] Browser tab title reads "Steel Designer" (browser-confirmed: `document.title`).
- [x] PDF report header string is "McVeigh Steel Designer" (source edit; static string, no logic).
- [x] No diagram draw logic in `pdfExport.ts` changed.
- [x] `tsc --noEmit` 0 errors; `npm run build` clean.

## Note
PDF header confirmed by code inspection + successful build (the value is a static literal with no
conditional path). Automated PDF-text extraction from the download was not performed; visually
confirmable by clicking "Export PDF Report".

## History
- Both string edits applied → title QA PASS; PDF header verified by code.
