# 4R5 — Expanded calculation working (Item 3)

## STATUS
- Column: DONE
- Agent: direct-exec
- Critic verdict: APPROVED (ResultsPanel.tsx only; reads results.intermediates, no new computation)
- TV result: PASS (tsc 0, build clean). 360UB44.7/8m/20kN·m: toggle expands all three sections; φM_sx 208.9 / φM_bx 58.0 match results table; βm/holes/2.5% notes present; Unicode φ λ α ψ ≤ ² √ β used. Axial 200kN → COMBINED ACTIONS appears (N*=240, φNc=205.4, αc=0.133, both FAIL); clearing/Hide collapses; button toggles ▾/▴.
- Commit: 3a89c60
- Evidence: .nova/evidence/rev4/4R5-calc-working.png

## Items implemented
- HANDOVER Item 3 — Collapsible expanded calculation working in Results panel

## Files owned
- `src/components/ResultsPanel.tsx` (collapsible calc section only)

## Overview
Collapsible section below the Results table, toggled by "Show calculations ▾ / Hide calculations ▴".
All values from `results.intermediates` — NO new engineering computation. Unicode symbols (φ, λ, α, ψ, ≤, ², √).

## Sections
- BENDING [util%]: classification (λ_f, λ_w, ε_p, ε_y, class), Ze, φMsx, restraint note, βm note,
  L_max, segment vs L_max, (if LTB) L_eb/M_oa/α_m/α_s/φM_bx
- SHEAR [util%]: A_w, d/t_w + limit + classification, φV_v
- DEFLECTION: live load category + ψ_l; G+ψ_l·Q δ/limit/PASS-FAIL; G δ/limit/PASS-FAIL
- COMBINED ACTIONS [util%]: only when nStar > 0 — section + member checks

## Critic checklist (card-specific)
- ONLY ResultsPanel.tsx modified
- BENDING, SHEAR, DEFLECTION all rendered
- COMBINED ACTIONS conditional (nStar > 0)
- Unicode symbols used
- Toggle text correct
- No new computation — intermediates only
- Assumption notes present (βm = −1.0, holes, 2.5% flange force)

## Visual confirmation steps (Test Validator — browser MCP required)
1. Configure 360UB44.7, 8m, 20 kN/m line load, Office.
2. "Show calculations ▾" button visible.
3. Click → section expands; BENDING/SHEAR/DEFLECTION visible.
4. BENDING shows λ_f, λ_w, class, Ze, φMs, notes, LTB working.
5. Unicode symbols present.
6. DEFLECTION shows live load category + ψ_l.
7. φMs in calc matches results table.
8. Add axial 200 kN → COMBINED ACTIONS appears; clear → disappears.
9. "Hide calculations ▴" collapses.

## History
(append-only)
