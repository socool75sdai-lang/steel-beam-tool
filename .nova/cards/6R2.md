# 6R2 — Column "Show calculations" collapsible — Item 3

## STATUS
- Column: BACKLOG
- Agent: (unassigned)
- Critic verdict: (pending)
- Validator result: (pending)
- Evidence: (pending)

**Files owned:** `src/components/ColumnResultsPanel.tsx`.

**Implementation:** add the same "Show calculations ▾/▴" collapsible the beam has — monospace block,
formula → substituted values → result, AS-clause tags — reading already-computed `ColumnIntermediates`
(no new computation). Cover: section classification (Cl. 5.2.2); axial section φNs (Cl. 6.2); member
buckling φNc x & y with λn/αc (Cl. 6.3.3); bending φMs/φMb (Cl. 5); combined Cl. 8.3.3/8.4.2/8.4.4.
On-screen text reads identically to the column PDF calc sheet. Grey captions on green use `.mc-subtle`;
calc-box body stays dark on white.

**Critic checklist:** collapsible mirrors beam structure/styling; content matches column PDF calc
sheet; reads existing intermediates; all AS clauses tagged; calc-box body dark on white; no
`any`/`@ts-ignore`.

**Visual confirmation:** Column tab, UC with axial + moment; expand collapsible → substituted values
match results table; matches PDF calc sheet; toggles correctly.

## History
