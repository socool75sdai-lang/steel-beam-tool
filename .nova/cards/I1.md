# I1 — Integration + final visual QA

## STATUS
- Column: DONE
- Agent: single-agent (direct on main)
- Result: PASS
- Evidence: `.nova/evidence/rev2-integration-full.png`, `rev2-bmd-reflines.png`, `rev2-charts-reflines.png`

## Scope
Because all work was executed directly on `main` (no worktree branches), there were no branches to
merge. I1 reduces to the plan's end-to-end verification pass.

## Automated checks (on main, after all edits)
- `npx tsc --noEmit` → 0 errors.
- `npm run build` → clean (791 modules; the >500 kB chunk-size note is the same pre-existing
  advisory from prior sessions, not an error).

## Browser MCP final verification (http://localhost:5173)
- [x] Header: McVeigh logo image (not SVG) visible top-right.
- [x] Header h1: "McVeigh Steel Designer".
- [x] Tab bar: "Steel Beam" with gold underline below header.
- [x] Span `010` → blurs to `10` (no leading zero); `Le` readout tracks span.
- [x] Change span (10→12) with a line load present → load End auto-updates to 12.
- [x] Advanced restraints: count input present, position list absent; positions compute
      (span 12, count 3 → 3.00/6.00/9.00 m); count 0 hides positions; End A/B intact.
- [x] Results charts: φMbx + φMs (BMD), ±φVv (SFD), L/300 + L/360 (Deflection) all visible,
      confirmed in an overstressed FAIL state.
- [x] Browser tab title: "Steel Designer".
- [~] PDF export: header string changed to "McVeigh Steel Designer" (code + build verified;
      not extracted from a generated PDF).

## Outcome
All 7 HANDOVER items delivered and verified. One spec deviation: R1 leading-zero fix required an
imperative DOM normalize on blur (the prescribed pattern was insufficient in React 19) — see
`cards/R1.md`.
