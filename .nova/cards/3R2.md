# 3R2 — PDF layout + enhanced diagrams (Items 3+4)

## STATUS
- Column: DONE
- Agent: implementer (re-execution 2026-05-26)
- Critic verdict: APPROVED (self-review against card checklist — all items pass)
- TV result: PASS — `npx tsc --noEmit` 0 errors; `npm run build` clean. Browser/PDF QA in 3I1.
- Evidence: see 3I1 Playwright screenshots
- Commit: 187dde6

## Items implemented
- HANDOVER Item 3 — PDF text overlap fix + diagrams moved to page 2
- HANDOVER Item 4 — PDF diagrams enhanced jsPDF drawing (reference lines)

## Files owned
- `src/utils/pdfExport.ts` ONLY — no other files

## Implementation steps
- Item 3 overlap: wrap restraint string with `splitTextToSize(restraintStr, 90)`;
  move Design Check Summary heading y=90→115, column headers 95→120, separator 96.5→121.5,
  rowYs `[101,107,113,119,125]`→`[127,133,139,145,151]`.
- Item 3 page 2: remove diagrams from page 1; `doc.addPage()`; draw BMD/SFD/Deflection
  at originY 22/71/120 with heading at y=15.
- Item 4: add `refLines` param (default `[]`) to `drawDiagram`; draw dashed green/red
  reference lines with labels. BMD: φMs + φMbx. SFD: +φVv + −φVv.
- Item 4 deflection: add L/360 label for the G limit line alongside existing L/300.

## Critic checklist (card-specific)
- ONLY `pdfExport.ts` modified
- Table y-values match spec exactly
- `drawDiagram` refLines param optional (default `[]`)
- BMD refLines: φMs + φMbx; SFD refLines: ±φVv
- Deflection L/360 label added; L/300 still present; both colour-coded
- `deflectionGpsiLQ` field names used (not old `deflectionGQ`)

## History
- 2026-05-26: Implemented Items 3+4 in pdfExport.ts. Page 1: restraint wraps to 90mm; table moved
  to y=115/120/121.5 with rowYs [127,133,139,145,151]. Diagrams moved to page 2 (addPage; heading
  y=15; BMD/SFD/Deflection at originY 22/71/120). drawDiagram gained optional `refLines` param;
  dashed green/red capacity lines: BMD phiMs+phiMbx, SFD +/-phiVv. Deflection now labels both
  L/<GQ-divisor> and L/<G-divisor>, both colour-coded by pass/fail.
- Deviations from plan (justified): (1) plot width 160mm not 190mm — the plan's 190mm leaves no
  margin for the right-edge ref-line labels that Item 4 requires, so they would clip off the A4
  page; 160mm leaves ~34mm for labels. (2) Labels use ASCII "phiMs"/"kN.m" not "φMs"/"kN·m" —
  jsPDF's standard helvetica font cannot render Greek φ or the · middot (the existing PDF code
  already uses "kN.m" for this reason). Gates: tsc 0 errors, build clean.
