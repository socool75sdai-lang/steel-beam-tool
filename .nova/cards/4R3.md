# 4R3 — Fixed end support conditions (Item 1)

## STATUS
- Column: DONE
- Agent: direct-exec
- Critic verdict: APPROVED (FEM via exact superposition + force-method propped values; PP unchanged; momentCapacity/shearCapacity untouched)
- TV result: PASS (tsc 0, build clean). 360UB44.7/10m/20kN·m UDL: M* PP 306.58 → FF 204.40 (=2/3, wL²/12 vs wL²/8); δ PP 109.97 → FF 21.99 (=1/5); αm 1.17→2.50. PF M*=306.59 (=wL²/8 propped, correct). FF locks both ends (2 disabled+notes); PF locks End B only (1); PP releases (0). FF BMD shows hogging ends + sagging midspan.
- Commit: 0a033ce
- Evidence: .nova/evidence/rev4/4R3-fixed-fixed.png, 4R3-ff-bmd.png

## Items implemented
- HANDOVER Item 1 — Fixed end support conditions (PP / FP / PF / FF)

## Files owned
- `src/types/index.ts`
- `src/engineering/as1170/loadCombinations.ts`
- `src/engineering/as4100/deflection.ts`
- `src/components/GeometryPanel.tsx`
- `src/components/RestraintPanel.tsx`
- `src/engineering/evaluate.ts`
- `src/utils/pdfExport.ts`

## Type additions (types/index.ts)
```typescript
export type SupportCondition = 'PP' | 'FP' | 'PF' | 'FF';
// Add to DesignInputs: supportCondition: SupportCondition;   // default: 'PP'
// Add to DesignIntermediates: supportCondition, femA, femB
```
Add `supportCondition: 'PP'` to App.tsx initial state.

## GeometryPanel.tsx — Support Conditions dropdown (below Span)
Options: PP (default), FP, PF, FF.

## RestraintPanel.tsx — lock/unlock logic
- FP/FF → End A fixed: Advanced mode, endA='F', disable + note "Set by support condition."
- PF/FF → End B fixed: same for endB.
- Switch back to PP: release locks (editable again).

## loadCombinations.ts — fixed-end moment analysis
After SS superposition, apply FEM correction:
`M(x) = M_ss(x) − FEM_A·(1 − x/L) − FEM_B·(x/L)`
Standard FEM tables for point/full-UDL/partial-UDL, PP/FP/PF/FF.
Reactions adjusted by `(FEM_B − FEM_A)/L` etc. PP: FEM=0 (unchanged).

## deflection.ts — fixed-end boundary conditions
PP unchanged; FF zero slope+disp both ends; FP/PF fixed end clamped, pin end free rotation.

## evaluate.ts
Pass `inputs.supportCondition` to analyseBeam + deflection; populate femA/femB/supportCondition in intermediates.

## pdfExport.ts — page 3
State support condition in Geometry/calc section.

## Critic checklist (card-specific)
- SupportCondition type + field on DesignInputs; default 'PP' unchanged end-to-end
- FEM formulas standard structural mechanics for all load types
- BMD correction applied after superposition
- Deflection updated for fixed-end BCs
- RestraintPanel grey-out + note; PP releases locks
- PDF page 3 states support condition
- DesignIntermediates extended (supportCondition, femA, femB)

## Visual confirmation steps (Test Validator — browser MCP required)
1. Navigate to localhost:5173
2. Confirm Support Conditions dropdown below Span.
3. Configure 360UB44.7, 10m span, 20 kN/m G UDL, PP. Note M*, deflection.
4. FF → BMD shows hogging at both ends, reduced sagging midspan.
5. Restraints panel End A/B greyed + note.
6. M* lower for FF than PP; deflection lower for FF.
7. PF → only End B locked; End A editable.
8. Back to PP → both editable; no regression.

## History
(append-only)
