# R2 — Restraint panel: blur fix + count-based intermediate spacing

## STATUS
- Column: DONE
- Agent: single-agent (direct on main)
- Critic verdict: APPROVED (1 cycle)
- TV result: PASS (browser-confirmed)
- Evidence: in-browser checks (positions text, count=0 hide, End A/B intact)

## Items implemented
- HANDOVER #1 — onBlur leading-zero fix on RestraintPanel number inputs (Le/L multiplier, αm override).
- HANDOVER #6 — Replace manual intermediate-position list with count-based equal spacing.

## Files owned
- `src/components/RestraintPanel.tsx`

## What was done
- Added `useState`/`useEffect` imports.
- Local `const [intermediateCount, setIntermediateCount] = useState(0)` (not in DesignInputs).
- `computedPositions = Array.from({length: count}, (_, i) => parseFloat(((span/(count+1))*(i+1)).toFixed(3)))`.
- `useEffect(() => updateRestraint({ intermediate: computedPositions }), [intermediateCount, inputs.span])`
  keeps the engineering layer's `intermediate: number[]` populated; recomputes on span change.
- New UI: single "Number of intermediate restraints" input (min 0, step 1) + read-only
  `Positions: …` preview shown only when count > 0.
- Removed `addIntermediate`, `updateInterm`, `removeInterm`, the `+ Add` button, and the mapped
  position-input list.
- onBlur on Le/L multiplier, αm override, and the count input also imperatively normalize the
  displayed value (same React-19 leading-zero fix as R1).

## Non-clobber
- End A / End B dropdowns in Advanced mode unchanged (browser-confirmed present).

## Acceptance checklist
- [x] Advanced mode: count input present; old position list + `+ Add` gone (browser-confirmed).
- [x] Span 12, count 3 → `Positions: 3.00 m, 6.00 m, 9.00 m`.
- [x] Count 2 → `Positions: 4.00 m, 8.00 m`; count 0 → positions text hidden.
- [x] Positions recompute when span changes (effect dep on `inputs.span`).
- [x] End A/End B dropdowns intact.
- [x] `tsc --noEmit` 0 errors; `npm run build` clean.

## History
- Implemented count-based spacing + blur fix → QA PASS.
