# R4 — App branding: logo, rename, tab bar, span-sync

## STATUS
- Column: DONE
- Agent: single-agent (direct on main)
- Critic verdict: APPROVED (1 cycle)
- TV result: PASS (browser-confirmed)
- Evidence: `.nova/evidence/rev2-integration-full.png`

## Items implemented
- HANDOVER #2 — McVeigh logo (replace inline SVG placeholder).
- HANDOVER #3 — Rename header `<h1>` to "McVeigh Steel Designer".
- HANDOVER #4 — Tab bar below header ("Steel Beam", gold underline).
- HANDOVER #7 — Span-sync: line/area load `end` fields follow span changes.

## Files owned
- `src/App.tsx`
- `public/logo.jpg` (copied from `.Improvements/Rev 2/2.jpg`)

## What was done
- Copied `2.jpg` → `public/logo.jpg`; replaced the inline `<svg>` wordmark with
  `<img src="/logo.jpg" alt="McVeigh Consultants" className="h-10 w-auto" />`.
- `<h1>` text → "McVeigh Steel Designer".
- Inserted `<nav>` between `<header>` and the flex-1 content: background `var(--mc-green-mid)`,
  `border-bottom: 2px solid var(--mc-gold)`, single active "Steel Beam" button with gold underline
  and `-mb-px` offset.
- `handleChange` now functional-update form: when `patch.span !== undefined`, maps every line and
  area load's `end` to the new span.

## Non-clobber
- Theme switcher dropdown (McVeigh / Light) and `--mc-*` CSS custom properties intact
  (browser-confirmed dropdown present).

## Acceptance checklist
- [x] Logo image renders in header (not SVG) — browser-confirmed top-right.
- [x] h1 reads "McVeigh Steel Designer" — browser-confirmed.
- [x] "Steel Beam" tab with gold underline below header — browser-confirmed.
- [x] Change span 10→12 with a line load present → load End auto-updates to 12 (browser-confirmed).
- [x] Point loads unaffected (no `end` field).
- [x] Theme dropdown intact.
- [x] `tsc --noEmit` 0 errors; `npm run build` clean.

## History
- Implemented all four sub-items + logo copy → QA PASS.
