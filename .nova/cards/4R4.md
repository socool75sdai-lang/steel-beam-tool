# 4R4 — Axial compression + combined actions (Item 2)

## STATUS
- Column: DONE
- Agent: direct-exec
- Critic verdict: APPROVED with deviation — replaced the plan's approximate calcAlphaC closed form with the exact AS4100 Cl.6.3.3 curve, since the plan's version gave αc≈0.85 at λn≈175 vs the cited Table 6.3.3(a) value ≈0.22 (the plan text mandates "αc from AS4100 Table 6.3.3(a)"). New file only; no existing engineering module modified.
- TV result: PASS (tsc 0, build clean). 360UB44.7/6m/10kN·m UDL: no compression → no combined rows. N*=100kN G (N*=120 factored): Section 0.348 PASS (=120/1544.4+56.37/208.89), Member 1.016 FAIL (φNc⇒αc≈0.225 ✓), overall FAIL. Clear axial → rows gone, Category disabled, overall PASS.
- Commit: 6fc90cb
- Evidence: .nova/evidence/rev4/4R4-combined-actions.png

## Items implemented
- HANDOVER Item 2 — Compressive axial load (beam-column), AS4100 Cl. 8.4

## Files owned
- `src/types/index.ts`
- `src/engineering/as4100/compressionCapacity.ts` (NEW FILE)
- `src/engineering/evaluate.ts`
- `src/components/LoadPanel.tsx`
- `src/components/ResultsPanel.tsx`
- `src/utils/pdfExport.ts`

## Type additions (types/index.ts)
- DesignInputs: `axialCompression: { magnitude: number; category: LoadCategory } | null`
- DesignIntermediates: nStar, phiNs, phiNc, kf, lambdaN, alphaC, combinedSectionRatio, combinedMemberRatio
- CapacityResults.passes: combinedSection, combinedMember

## compressionCapacity.ts (NEW)
`calcCompressionCapacity(section, fy, Le_mm, sectionClass): CompressionResult`
- φNs = 0.9·Ag·fy/1000 (kN) [Cl. 6.2]
- kf = 1.0 (Aeff not in DB → conservative)
- rMin = √(min(Ix,Iy)/Ag)
- λn = (Le/rMin)·√(kf·fy/250)
- αc Perry-Robertson HR category [Table 6.3.3(a)]
- φNc = 0.9·αc·kf·Ag·fy/1000 (kN)

## evaluate.ts
Compute N* = max over combos. Call calcCompressionCapacity when axialCompression set.
- combinedSectionRatio = N*/φNs + M*/φMs
- combinedMemberRatio = N*/φNc + M*/φMbx
- passes.combinedSection / combinedMember (true when no compression)

## LoadPanel.tsx
Axial Compression (kN) input + Category G/Q select at bottom of load area. null when blank/0.

## ResultsPanel.tsx
Two conditional rows (Combined Section / Combined Member) when nStar > 0; feed overall PASS/FAIL.

## pdfExport.ts
Page 3 "COMBINED ACTIONS [AS4100 Cl. 8.4]" section, conditional on N* > 0.

## Critic checklist (card-specific)
- axialCompression is `... | null`
- compressionCapacity.ts NEW; no existing engineering module modified
- φNs / φNc formulas correct (Cl. 6.2 / 6.3)
- Combined ratios units consistent (kN, kN·m)
- Results rows hidden when null; feed overall PASS/FAIL
- PDF section conditional
- DesignIntermediates extended with 8 new fields

## Visual confirmation steps (Test Validator — browser MCP required)
1. localhost:5173; Load panel shows Axial Compression row.
2. Beam 360UB44.7, 6m, 10 kN/m G UDL → no combined rows.
3. Axial 100 kN G → combined rows appear with numeric values.
4. Plausible non-NaN util.
5. Increase axial → combined FAIL + overall FAIL.
6. Clear axial → rows disappear.
7. Export PDF → page 3 COMBINED ACTIONS section visible.

## History
(append-only)
