# 4R2 — Job metadata + save/load JSON (Items 4+5)

## STATUS
- Column: DONE
- Agent: direct-exec
- Critic verdict: APPROVED (buildFilename shared from pdfExport.ts — within owned files; pages 2/3 untouched, page-1 job block shifts content by tShift only)
- TV result: PASS (tsc 0, build clean; export J001_TownHall_20260527_2024.json with correct {jobNumber,jobName,inputs} schema; reload cleared; import restored J001/Town Hall/360UB44.7 with "Loaded:" msg; PDF J001_TownHall_20260527_2025.pdf)
- Commit: b2858fa
- Evidence: .nova/evidence/rev4/4R2-import-roundtrip.png

## Items implemented
- HANDOVER Item 4 — Job Number and Job Name fields
- HANDOVER Item 5 — Save / Load JSON file (import/export)

## Files owned
- `src/App.tsx`
- `src/utils/pdfExport.ts`
- `src/components/ResultsPanel.tsx`

## App.tsx changes
- State: `jobNumber`, `jobName` (useState('')), `importMsg`, `fileInputRef`
- Layout: Job fields row + Import/Export button row, below header, above input panels
- Export handler: `{ jobNumber, jobName, inputs }` → JSON blob download via buildFilename
- Import handler: parse JSON, restore jobNumber/jobName/inputs, inline message
- Filename builder: `[JobNo]_[JobName]_YYYYMMDD_HHMM.{ext}`, blank parts omitted, safe chars
- Pass jobNumber/jobName down to ResultsPanel

## ResultsPanel.tsx changes
Accept `jobNumber`/`jobName` props; pass them into the `exportToPDF` call.

## pdfExport.ts changes
- PdfExportArgs: add `jobNumber?`, `jobName?`
- Page 1: job metadata block at top (before inputs), only when at least one field non-blank
- Filename: use buildFilename → `[JobNo]_[JobName]_YYYYMMDD_HHMM.pdf`

## Critic checklist (card-specific)
- `jobNumber`/`jobName` are React state (`useState('')`); no localStorage
- Both reset to empty on page load
- Import restores jobNumber, jobName, AND all DesignInputs
- Import success: "Loaded: {filename}"; failure: "Invalid file — could not load."
- Export JSON structure: `{ jobNumber, jobName, inputs }`
- Filename: blank parts omitted, no double underscore, only `[A-Za-z0-9_-]`
- PDF page 1 job block only when a field is non-blank
- pdfExport.ts: only page 1 job block + filename changed; pages 2/3 unchanged
- ResultsPanel.tsx: only the exportToPDF call site updated

## Visual confirmation steps (Test Validator — browser MCP required)
1. Navigate to `http://localhost:5173`
2. Confirm Job No / Job Name fields visible below header, above input panels.
3. Confirm Import/Export buttons visible in the row below.
4. Enter Job No "J001", Job Name "Town Hall"; configure a beam; click Export.
5. Confirm `.json` downloads named `J001_TownHall_YYYYMMDD_HHMM.json`.
6. Reload — confirm job fields empty (stateless).
7. Import the JSON; confirm "Loaded: {filename}".
8. Confirm Job No/Name and all inputs restored.
9. Export PDF — filename `J001_TownHall_YYYYMMDD_HHMM.pdf`; page 1 has job block.
10. Blank fields: PDF filename `YYYYMMDD_HHMM.pdf`, no job block.

## History
(append-only)
