# 3R1 — Engineering corrections (Items 1+2)

## STATUS
- Column: DONE
- Agent: implementer (re-execution 2026-05-26)
- Critic verdict: APPROVED (self-review against card checklist — all items pass)
- TV result: PASS — `npx tsc --noEmit` 0 errors; `npm run build` clean. Browser QA in 3I1.
- Evidence: see 3I1 Playwright screenshots
- Commit: 6c7d5ae

## Items implemented
- HANDOVER Item 1 — Deflection combination G+Q → G+ψ_l·Q
- HANDOVER Item 2 — Point load position: metres → % of span

## Files owned
- `src/types/index.ts`
- `src/engineering/as1170/psiFactors.ts` (NEW FILE — create it)
- `src/engineering/as1170/loadCombinations.ts`
- `src/engineering/as4100/deflection.ts`
- `src/engineering/evaluate.ts`
- `src/components/LoadPanel.tsx`
- `src/components/ResultsPanel.tsx`
- `src/App.tsx`
- `src/utils/pdfExport.ts` (2 label strings only)

## Implementation steps
- types: add `LiveLoadType`; add `liveLoadType` to `DesignInputs`; rename
  `CapacityResults.deflectionGQ`→`deflectionGpsiLQ`, `deflectionLimitGQ`→`deflectionLimitGpsiLQ`,
  `passes.deflectionGQ`→`deflectionGpsiLQ`; rename `DiagramSet.deflectionGQ`→`deflectionGpsiLQ`.
- psiFactors.ts: PSI_L_FACTORS, LIVE_LOAD_LABELS, getPsiL.
- deflection.ts: combo `'G+ψ_l·Q' | 'G'`, psiL param, qFactor = psiL; position `(pl.position/100)*span*1000` in both functions.
- loadCombinations.ts: point position `(p.position/100)*span`.
- evaluate.ts: pass psiL, rename fields.
- LoadPanel.tsx: liveLoadType dropdown at top; position header "Position (% span)"; validation 0–100; placeholder "% of span".
- ResultsPanel.tsx: dynamic `Deflection (G+{psiL}Q)` label; rename refs.
- App.tsx: `liveLoadType: 'office'`.
- pdfExport.ts: deflection label dynamic; point load `${p.position.toFixed(0)}%`.

## Critic checklist (card-specific)
- ψ_l values match AS1170.1 Table 4.1 (0.4 domestic/office/parking/retail, 0.6 storage, 0.0 roof)
- Both `calcDeflection` AND `calcDeflectionProfile` converted for position AND psiL
- `loadCombinations.ts` point position uses `(p.position/100)*span`
- Validation in LoadPanel changed to 0–100 range
- `DeflLimits.GQ` field name unchanged (divisor, not combo name)
- No references to old `deflectionGQ` field name remain
- `App.tsx` default state has `liveLoadType: 'office'`
- `pdfExport.ts` ONLY modified for 2 label strings — no layout changes

## History
- 2026-05-26: Implemented Items 1+2. Added `LiveLoadType` + `psiFactors.ts`; `liveLoadType` on
  `DesignInputs` (default 'office' in App.tsx). Deflection combo `'G+ψ_l·Q'` with `psiL` param in
  both `calcDeflection`/`calcDeflectionProfile`; point position converted `(pos/100)*span` in
  deflection.ts + loadCombinations.ts. Renamed `deflectionGQ`→`deflectionGpsiLQ` (and limit/passes)
  across types, evaluate, ResultsPanel, pdfExport. LoadPanel: live-load dropdown at top, position
  header "Position (% span)", validation 0–100, placeholder "% of span". pdfExport: dynamic
  `G+{psiL}Q` label + point load `${pos}%`. Gates: tsc 0 errors, build clean.
