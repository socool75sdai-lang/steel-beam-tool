# 6R4 — Brace UI + hook + tab + graphs + collapsible — Item 2 (UI) + Item 3 (Brace)

## STATUS
- Column: DONE
- Agent: Implementer (inline)
- Critic verdict: APPROVED — tab order Beam|Column|Brace, all three always mounted (CSS hidden); default CHS/C350; auto-lightest searches 3 types × 3 grades and sets type+grade+size; Wind signed; self-weight read-only; live-load drives psi_c; deflection limit editable default 360; three graphs; collapsible matches PDF content; PDF stubbed; beam/column unaffected; no any/@ts-ignore
- Validator result: PASS — Brace tab full UI; live engine matches hand-calc EXACTLY (phiNs=1026.0, phiNc=679.8, phiMsx=51.03, delta=0.39 mm, sw=0.251 kN/m); three graphs render; auto-lightest returned global lightest CHS48.3x3.2 (CHS, C250, 3.6 kg/m) setting type+grade+size
- Evidence: .nova/evidence/rev6/6R4-brace-results.png
- Commit: 813c7cd

**Files owned:** `src/BraceApp.tsx` (NEW), `components/BraceGeometryPanel.tsx` /
`BraceLoadPanel.tsx` / `BraceResultsPanel.tsx` (NEW), `hooks/useBraceCalculations.ts` (NEW),
`src/App.tsx`.

**Steps:**
- `useBraceCalculations`: `useMemo(() => evaluateBrace(inputs), [inputs])`.
- `App.tsx`: extend `activeTab` to `'beam'|'column'|'brace'`; nav order Steel Beam | Steel Column |
  Steel Brace; mount `<BraceApp>` always, `hidden` when inactive; thread jobNumber/jobName. JSON
  Save/Load stays beam-only.
- `BraceGeometryPanel`: span L; type CHS/SHS/RHS; grade C250/C350/C450 (default CHS/C350); size
  dropdown; kx, ky (default 1.0); read-only Le_x/Le_y. **Auto-lightest** searches full matrix
  {CHS,SHS,RHS}×{C250,C350,C450}, sets type+grade+size to global lightest passing.
- `BraceLoadPanel`: axial N*; transverse point-load editor (position, magnitude, tag G/Q/Wind, Wind
  signed); read-only self-weight UDL row; live-load category dropdown; deflection-limit field
  (default 360).
- `BraceResultsPanel`: PASS/FAIL banner; check table (axial φNs, member x&y φNc, bending φMs,
  combined Cl. 8.3.3/8.4.2/8.4.4, deflection); summary line (`.mc-subtle`); "Show calculations"
  collapsible (Item 3 for brace); three graphs (N–M interaction, BMD governing combo, deflection
  profile w/ L/limit ref line); Export PDF button (stub here, wired 6R5).

**Critic checklist:** tab order Beam|Column|Brace; all three always mounted (CSS hidden); default
CHS/C350; auto-lightest searches 3 types × 3 grades sets type+grade+size; Wind signed; self-weight
read-only; live-load drives ψc; deflection limit editable default 360; three graphs; collapsible
matches PDF; PDF stubbed; beam/column unaffected; no `any`/`@ts-ignore`; no npm packages.

**Visual confirmation:** Brace tab full UI; CHS + N* + Q + Wind-uplift → PASS/FAIL + check table;
three graphs render; auto-lightest returns global lightest sets type+grade+size; collapsible matches
table; beam/column state preserved on switch.

## History
