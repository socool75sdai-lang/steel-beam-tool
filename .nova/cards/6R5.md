# 6R5 — Brace PDF (3-page ASCII) — Item 2 (PDF)

## STATUS
- Column: DONE
- Agent: Implementer (inline)
- Critic verdict: APPROVED — new file; pdfExport.ts/columnPdfExport.ts untouched; ASCII-only (phi/lambda/psi/sqrt/<=); y>270 overflow guard; 3 pages (inputs+table | interaction+BMD+deflection | calc sheet); graphs drawn with jsPDF lines; uses buildFilename; calc sheet matches on-screen collapsible; only PDF button wired
- Validator result: PASS — Export PDF downloaded valid %PDF-1.3, exactly 3 pages, no runtime error
- Evidence: .nova/evidence/rev6/6R5-brace-export.pdf
- Commit: 06ee913

**Files owned:** `src/utils/bracePdfExport.ts` (NEW), `components/BraceResultsPanel.tsx` (wire PDF
button only).

**Steps:** 3-page A4 jsPDF export, ASCII-only ("phi"/"lambda"/"psi"/"sqrt"/"<="), `y > 270`
overflow guard. Page 1: inputs + check table. Page 2: graphs (interaction + BMD + deflection, jsPDF
lines, no chart lib). Page 3: calc sheet with AS-clause tags, reading identically to the on-screen
brace collapsible. Use shared `buildFilename(jobNumber, jobName, 'pdf')`; receive jobNumber/jobName
from `App`. Wire Export PDF button in `BraceResultsPanel`.

**Critic checklist:** new file; `pdfExport.ts`/`columnPdfExport.ts` untouched; ASCII-only; overflow
guard; 3 pages with specified content; graphs jsPDF lines; uses `buildFilename`; calc sheet matches
collapsible; only PDF button wired.

**Visual confirmation:** configure brace, Export PDF → download; 3 pages (inputs+table; three
graphs w/ design point + ref line; calc sheet ASCII-only w/ AS tags); page-3 matches collapsible;
beam + column PDF regression unaffected.

## History
