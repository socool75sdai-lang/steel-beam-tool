# 6R3 — Brace types + engine + psi_c — Item 2 (engine)

## STATUS
- Column: BACKLOG
- Agent: (unassigned)
- Critic verdict: (pending)
- Validator result: (pending)
- Evidence: (pending)

**Files owned:** `src/types/brace.ts` (NEW), `engineering/as4100/braceCapacity.ts` (NEW),
`engineering/as1170/psiFactors.ts`.

**psiFactors.ts:** add `getPsiC(type)` / `PSI_C_FACTORS` (AS1170.0 Table 4.1) alongside ψl. Reuse
`LiveLoadType`.

**types/brace.ts:** `BraceInputs` (span L m; section type CHS/SHS/RHS + grade C250/C350/C450 default
CHS/C350; kx, ky default 1.0; axial nStar single factored; transverse point loads [position m,
magnitude kN, tag G/Q/Wind — Wind signed +down/−up]; live-load category; deflection limit default
360). `BraceResults` mirrors `ColumnResults` (φNs, φNcX/Y/φNc, φMsx, φMbx=φMsx, three combined
ratios) plus governing M* + combo, BMD array, self-weight deflection profile + δ + limit,
`BraceIntermediates`.

**braceCapacity.ts:** `evaluateBrace(inputs)`. Axial: reuse column path (calcFyHollow, calcAlphaC,
rx=√(Ix/Ag), ry=√(Iy/Ag), Le_x=kx·L, Le_y=ky·L → φNcX/Y/min, φNs). Bending (major only): self-weight
UDL (calcSelfWeightKnPerM, G) + transverse point loads; worst |M*| of 1.2G+1.5Q | 1.2G+Wu+ψc·Q |
0.9G+Wu; BMD for governing combo; M*y=0. φMbx=φMsx (no LTB note). Combined Cl. 8.3.3/8.4.2/8.4.4 with
M*y=0. Deflection: self-weight UDL, simply-supported, vs L/limit.

**Critic checklist:** ψc added without altering ψl; types defined; default CHS/C350, limit 360;
single N* in all combos; bending = self-weight + points, worst of 3; φMbx=φMsx; combined reuses
column Cl. 8.3/8.4; deflection self-weight only; reuses calcFyHollow/calcAlphaC/calcSelfWeightKnPerM;
no `any`/`@ts-ignore`; no npm packages.

**Verification (no browser):** hand-calc spot-check a CHS → reproduce N*, governing M* + combo,
φNcX/Y, Cl. 8.3/8.4 ratios, L/360 deflection. Record below.

## History
