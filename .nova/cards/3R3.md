# 3R3 — PDF calc sheet (Item 5)

## STATUS
- Column: DONE
- Agent: implementer (re-execution 2026-05-26)
- Critic verdict: APPROVED (self-review against card checklist — all items pass)
- TV result: PASS — `npx tsc --noEmit` 0 errors; `npm run build` clean. Browser/PDF QA in 3I1.
- Evidence: see 3I1 Playwright screenshots
- Commit: 319301f

## Items implemented
- HANDOVER Item 5 — Running calculation sheet (PDF page 3+)

## Files owned
- `src/types/index.ts` (add DesignIntermediates + extend CapacityResults)
- `src/engineering/as4100/momentCapacity.ts`
- `src/engineering/as4100/shearCapacity.ts`
- `src/engineering/evaluate.ts`
- `src/utils/pdfExport.ts` (page 3+ only — do NOT change page 1 or 2)

## Implementation steps
- types: add `DesignIntermediates` interface (per HANDOVER spec); add
  `intermediates: DesignIntermediates` to `CapacityResults`.
- momentCapacity.ts: extend `classifySection` to return flange/web Ep/Ey; extend
  `calcSectionCapacity` to include them; extend `calcMemberCapacity` to expose `Moa`, `alphaM`.
- shearCapacity.ts: extend `ShearResult`/return with `dOnTw`, `slenderLimit`.
- evaluate.ts: assemble `intermediates` from augmented returns + LIVE_LOAD_LABELS.
- pdfExport.ts: page 3 calc sheet (13 steps) with `checkOverflow()` (y>270→addPage, cont. header);
  extend `PdfExportArgs` with `intermediates`; ResultsPanel call site passes `results.intermediates`.

## Critic checklist (card-specific)
- `DesignIntermediates` matches HANDOVER spec exactly
- `CapacityResults.intermediates` non-optional
- momentCapacity/shearCapacity return objects EXTENDED, not replaced
- All 13 calc steps rendered on page 3+
- `checkOverflow()` after every step; resets cy on new page
- Page 3 header + continuation "(cont.)" header
- Page 1 and page 2 content unchanged

## History
- 2026-05-26: Implemented Item 5. Added `DesignIntermediates` interface + non-optional
  `intermediates` on `CapacityResults`. Extended (not replaced) `ClassificationResult` and
  `SectionCapacityResult` with flange/web Ep/Ey; `MemberCapacityResult` already exposed `Moa`/
  `alphaS`. Extended `ShearResult` with `dOnTw`/`slenderLimit`. evaluate.ts assembles the full
  intermediates block (LIVE_LOAD_LABELS for the type label). pdfExport.ts renders page 3 calc
  sheet: 13 numbered steps with AS clause tags, formula + substituted-value + result lines,
  per-step separators, and `checkOverflow()` (y>270 -> addPage, cy=20, "(cont.)" header).
- Deviations from plan (justified): (1) `PdfExportArgs` was NOT extended and the ResultsPanel
  call site was NOT changed — `intermediates` lives on `CapacityResults`, which `exportToPDF`
  already receives via `results`, so it is read as `results.intermediates`. This is cleaner than
  the plan's separate arg and avoids touching a non-owned file. (2) Math notation is ASCII
  (lambda, sqrt, phi, ^2, <=, kN.m) because jsPDF's standard font cannot render Greek/symbol
  glyphs. (3) With the standard 13-step content the sheet fits on page 3; the overflow guard is
  implemented per spec and triggers only if content exceeds y=270. Gates: tsc 0 errors, build clean.
