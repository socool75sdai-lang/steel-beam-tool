# 5R4 — Column PDF export (Item 4c)

## STATUS
- Column: IN_PROGRESS (code complete; visual QA deferred to 5I1)
- Agent: implementer (direct execution)
- Commit: b4008e0
- Critic verdict: self-reviewed against checklist — PASS
- TV result: tsc 0 errors, build clean; PDF output verified ASCII-only (grep); browser confirmation in 5I1
- Evidence: (5I1 integrated session)

## Implementation notes
- columnPdfExport.ts NEW; pdfExport.ts NOT modified (buildFilename imported/reused only).
- ASCII-only confirmed via grep -P "[^\x00-\x7F]" (only matches were em-dashes in code comments, since replaced; no Unicode in PDF output strings).
- y>270 overflow guard via checkOverflow() on page 3 calc sheet.
- Page 1: design inputs + section properties (J/Iw for UC only) + 8-row check table.
- Page 2: interaction diagram via jsPDF lines + filled circle design point.
- Page 3: 13 numbered steps with AS clause tags; step 5 = "No LTB - closed section" for hollow, full LTB working for UC.
- jobNumber/jobName threaded App.tsx -> ColumnApp -> ColumnResultsPanel (additive props; beam untouched).

## Items implemented
- HANDOVER Item 4 (PDF export) — 3-page jsPDF column export; wire up PDF button in ColumnResultsPanel

## Files owned
- `src/utils/columnPdfExport.ts` (NEW FILE)
- `src/components/ColumnResultsPanel.tsx` (wire up PDF button only)

## Implementation notes

### columnPdfExport.ts (NEW FILE)
3-page A4 jsPDF export per HANDOVER §4.6. Follow exact same coding conventions as `pdfExport.ts`:
- No external charting libraries — jsPDF line drawing only for all diagrams
- ASCII only: no Unicode phi, lambda, squared, sqrt, <= — use "phi", "lambda", "sqrt", "<="
- y > 270 overflow guard with addPage() + y = 10 reset throughout
- Same font sizes and line heights as beam export

**Page 1 — Inputs + properties + check table**
- Job block (if jobNumber/jobName provided)
- Design inputs: height, section designation, grade, fy, k_x, k_y, Le_x, Le_y, G, Q, N*, e_x, e_y, M*x, M*y
- Section properties: Ag, Ix, Iy, rx, ry (+ J, Iw for UC if in section DB)
- Capacity check table (8 rows matching UI table)

**Page 2 — Interaction diagram**
- N*-M* interaction diagram for governing axis using jsPDF line drawing
- Section envelope (straight line), member envelope (straight line), design point (filled circle)
- Title: "N*-M*x Interaction Diagram (AS4100)" or y-axis equivalent
- Axis labels: "M* (kN.m)", "N* (kN)"

**Page 3 — Calculation sheet**
13 numbered steps with AS clause tags in brackets:
1. Steel grade + fy [AS3678 / AS1163]
2. Section classification [AS4100 Cl. 5.2.2]
3. Ze_x, Ze_y [AS4100 Cl. 5.2.3-5.2.5]
4. phi*Msx, phi*Msy [AS4100 Cl. 5.2.1]
5. LTB: UC shows Moa/alphas/phi*Mbx; hollow shows "No LTB - closed section" [AS4100 Cl. 5.6]
6. Factored loads N*, M*x, M*y [AS1170.1]
7. kf, phi*Ns [AS4100 Cl. 6.2.1-6.2.2]
8. lambda_n_x, alpha_c_x, phi*Nc_x [AS4100 Cl. 6.3.3]
9. lambda_n_y, alpha_c_y, phi*Nc_y [AS4100 Cl. 6.3.3]
10. Combined - section Cl. 8.3.3 [AS4100 Cl. 8.3.3]
11. Combined - member in-plane Cl. 8.4.2 [AS4100 Cl. 8.4.2]
12. Combined - member out-of-plane Cl. 8.4.4 [AS4100 Cl. 8.4.4]
13. Overall result

### ColumnResultsPanel.tsx — wire up PDF button
Replace disabled stub with working onClick calling `exportColumnToPDF(inputs, results, jobNumber, jobName)`.
Accept `jobNumber` and `jobName` as optional props passed from ColumnApp.tsx.
ColumnApp.tsx should receive jobNumber/jobName from App.tsx (App.tsx manages both job fields).

## Critic checklist (card-specific)
- `columnPdfExport.ts` is a NEW file; `pdfExport.ts` is NOT modified
- ASCII only throughout: no phi, lambda, squared, sqrt, <= in PDF output
- y > 270 overflow guard present on all three pages
- Page 1: all design input fields + section properties + 8-row check table
- Page 2: interaction diagram drawn with jsPDF lines (not canvas screenshot or external lib)
- Page 2: design point rendered (filled circle or cross) at (M*_gov, N*)
- Page 3: all 13 numbered steps with AS clause annotations in brackets
- Step 5: "No LTB - closed section" for hollow; full Moa/alphas working for UC
- `ColumnResultsPanel.tsx`: only PDF button onClick wired up; no other structural changes
- `pdfExport.ts` untouched (beam PDF unaffected)

## Visual confirmation steps (Test Validator — browser MCP required)

1. Ensure dev server running at `http://localhost:5173`.
2. Navigate to Steel Column tab.
3. Configure: Height=5m, UC, Grade 350, 200UC52.2, k_x=k_y=1.0, G=300 kN, Q=200 kN, e_y=30mm.
4. Click "Export PDF" — confirm PDF downloads (3 pages).
5. Take screenshot of PDF page 1 (`5R4-pdf-page1.png`) — inputs, section properties, 8-row table,
   no text overlap.
6. Take screenshot of PDF page 2 (`5R4-pdf-page2.png`) — N*-M* interaction diagram with two
   capacity lines and design point drawn.
7. Take screenshot of PDF page 3 (`5R4-pdf-page3.png`) — 13 numbered steps, AS clause tags,
   no Unicode symbols (confirm no phi/lambda/sqrt characters).
8. Switch to SHS section, Grade 450 — confirm page 3 step 5 shows "No LTB - closed section".
9. Beam regression: switch to beam tab, export PDF — confirm beam PDF still 3 pages, correct layout.

## History
(append-only)
